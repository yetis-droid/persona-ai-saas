# 💰 Stripe本番設定完全ガイド（自動振込設定）

このガイドに従えば、**あなたの銀行口座に自動で収益が振り込まれる**ようになります。

---

## 🎯 最終目標

```
ユーザーがPremiumプラン購入（¥980/月）
    ↓
Stripe が自動決済
    ↓
あなたの銀行口座に自動振込（毎月）
```

---

## ✅ **STEP 1: Stripeアカウント作成（5分）**

### 1-1. Stripeに登録

1. https://dashboard.stripe.com/register にアクセス
2. メールアドレスを入力して「今すぐ開始」
3. パスワード設定
4. メール認証

### 1-2. ビジネス情報の登録

```
ビジネスタイプ: 個人事業主
ビジネス名: Persona AI
業種: ソフトウェア開発 / SaaS
ウェブサイト: https://persona-ai.pages.dev
```

### 1-3. 銀行口座の登録（重要！）

**ここで登録した銀行口座に自動振込されます**

1. Stripe ダッシュボード → **設定** → **支払い設定**
2. **振込先銀行口座** をクリック
3. 以下を入力：
   ```
   銀行名: （あなたの銀行）
   支店コード: （あなたの支店番号）
   口座種別: 普通/当座
   口座番号: （あなたの口座番号）
   口座名義: （カタカナで入力）
   ```
4. **保存**

**振込スケジュール**:
- デフォルト: **週1回**（毎週火曜日）
- 変更可能: 日次、週次、月次

---

## 🔑 **STEP 2: API キーの取得**

### 2-1. テストモードキー（開発用）

1. Stripe ダッシュボード → **開発者** → **APIキー**
2. **テストモードの表示**をONにする
3. 以下をコピー：
   - **公開可能キー**: `pk_test_...`
   - **シークレットキー**: `sk_test_...`

### 2-2. 本番モードキー（実際の決済用）

1. Stripe ダッシュボード → **開発者** → **APIキー**
2. **本番モードの表示**をONにする
3. 以下をコピー：
   - **公開可能キー**: `pk_live_...`
   - **シークレットキー**: `sk_live_...`

**⚠️ 重要**: `sk_live_...` は絶対に公開しないでください！

---

## 🔧 **STEP 3: バックエンドの環境変数設定**

### 3-1. 開発環境（サンドボックス）

```bash
cd /home/user/webapp/persona-ai-saas/backend

# .env ファイルに追加
cat >> .env << 'EOL'

# Stripe テストモード
STRIPE_SECRET_KEY=sk_test_あなたのシークレットキー
STRIPE_PUBLISHABLE_KEY=pk_test_あなたの公開可能キー
STRIPE_WEBHOOK_SECRET=whsec_あとで設定（STEP 4）
EOL
```

### 3-2. 本番環境（Railway）

**本番デプロイ後に設定**：

```bash
# Railway CLI で環境変数を設定
railway variables set STRIPE_SECRET_KEY=sk_live_あなたのシークレットキー
railway variables set STRIPE_PUBLISHABLE_KEY=pk_live_あなたの公開可能キー
railway variables set STRIPE_WEBHOOK_SECRET=whsec_本番Webhook署名シークレット
```

---

## 🎯 **STEP 4: Stripe商品・価格の作成**

### 4-1. Premium プランの作成

1. Stripe ダッシュボード → **商品**
2. **商品を追加** をクリック
3. 以下を入力：
   ```
   商品名: Persona AI Premium
   説明: 100回/日の会話、無制限の人格、広告なし
   価格: ¥980
   請求サイクル: 月次
   ```
4. **商品を作成**
5. **価格ID** をコピー（`price_...` で始まる）

### 4-2. バックエンドに価格IDを設定

```typescript
// backend/src/routes/subscription.ts
const STRIPE_PRICE_ID = 'price_あなたの価格ID'; // ここに貼り付け
```

---

## 🔔 **STEP 5: Webhookの設定（自動処理）**

Webhookは、Stripeから「決済が成功した」「サブスク継続」などのイベントを受け取る仕組みです。

### 5-1. 開発環境（ローカル）

**Stripe CLI をインストール**:

```bash
# Mac
brew install stripe/stripe-cli/stripe

# Windows / Linux
# https://stripe.com/docs/stripe-cli からダウンロード
```

**Stripe CLI でログイン**:

```bash
stripe login
```

**ローカルWebhookの起動**:

```bash
cd /home/user/webapp/persona-ai-saas/backend
stripe listen --forward-to localhost:3001/api/webhooks/stripe
```

**Webhook署名シークレットをコピー**:
```
Ready! Your webhook signing secret is whsec_...（これをコピー）
```

**.env に追加**:
```bash
STRIPE_WEBHOOK_SECRET=whsec_コピーしたシークレット
```

### 5-2. 本番環境（Railway デプロイ後）

1. Stripe ダッシュボード → **開発者** → **Webhook**
2. **エンドポイントを追加** をクリック
3. 以下を入力：
   ```
   エンドポイントURL: https://your-backend.railway.app/api/webhooks/stripe
   説明: Persona AI Production Webhook
   イベント選択:
     - checkout.session.completed（決済完了）
     - customer.subscription.created（サブスク作成）
     - customer.subscription.updated（サブスク更新）
     - customer.subscription.deleted（サブスクキャンセル）
     - invoice.paid（請求書支払い完了）
     - invoice.payment_failed（支払い失敗）
   ```
4. **エンドポイントを追加**
5. **署名シークレット** をコピー（`whsec_...`）
6. Railway で環境変数を更新：
   ```bash
   railway variables set STRIPE_WEBHOOK_SECRET=whsec_本番シークレット
   ```

---

## 💳 **STEP 6: フロントエンドの設定**

### 6-1. 公開可能キーを設定

```bash
cd /home/user/webapp/persona-ai-saas/frontend

# .env ファイルに追加（開発用）
cat >> .env << 'EOL'
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_あなたの公開可能キー
EOL
```

### 6-2. Cloudflare Pages に設定（本番用）

```bash
npx wrangler pages secret put VITE_STRIPE_PUBLISHABLE_KEY --project-name persona-ai-saas
# 値: pk_live_あなたの公開可能キー
```

---

## 🧪 **STEP 7: テスト決済**

### 7-1. テストカード番号

**成功するカード**:
```
カード番号: 4242 4242 4242 4242
有効期限: 任意の未来の日付（例: 12/34）
CVC: 任意の3桁（例: 123）
郵便番号: 任意（例: 123-4567）
```

**失敗するカード**（テスト用）:
```
カード番号: 4000 0000 0000 0002
→ カードが拒否されました
```

### 7-2. テスト手順

1. ブラウザで https://5173-iv30mcnq8rixy3ytf59wn-2b54fc91.sandbox.novita.ai/pricing にアクセス
2. **Premiumで始める** をクリック
3. テストカード情報を入力
4. **支払う** をクリック
5. ✅ 決済成功！
6. Stripe ダッシュボードで確認：
   - **支払い** → 決済履歴に表示される
   - **顧客** → 新しい顧客が追加される
   - **サブスクリプション** → アクティブなサブスクが表示される

---

## 💰 **STEP 8: 本番モードに切り替え（お金が入る！）**

### 8-1. Stripeで本番モードを有効化

1. Stripe ダッシュボード → 右上の **テストモード** トグルをOFF
2. **本番データのアクティブ化** をクリック
3. 身分証明書を提出（必要な場合）
4. 承認されるまで待つ（数時間〜1日）

### 8-2. 本番APIキーに切り替え

**バックエンド（Railway）**:
```bash
railway variables set STRIPE_SECRET_KEY=sk_live_あなたのシークレットキー
railway variables set STRIPE_PUBLISHABLE_KEY=pk_live_あなたの公開可能キー
```

**フロントエンド（Cloudflare Pages）**:
```bash
npx wrangler pages secret put VITE_STRIPE_PUBLISHABLE_KEY --project-name persona-ai-saas
# 値: pk_live_あなたの公開可能キー
```

### 8-3. 再デプロイ

```bash
# バックエンド
cd /home/user/webapp/persona-ai-saas/backend
railway up

# フロントエンド
cd /home/user/webapp/persona-ai-saas/frontend
npm run build
npx wrangler pages deploy dist --project-name persona-ai-saas
```

---

## 🎉 **完成！収益が自動で入ります**

### **収益の流れ**

1. **ユーザーが課金**（¥980/月）
2. **Stripe が決済処理**（自動）
3. **手数料が差し引かれる**（3.6%）
   - あなたの受取額: ¥945/月/人
4. **毎週火曜日に自動振込**（デフォルト）
   - 最低振込額: ¥100,000
   - それ以下の場合は次回に繰り越し

### **振込スケジュール例**

```
月曜日: ユーザーA が ¥980 課金
火曜日: ユーザーB が ¥980 課金
水曜日: ユーザーC が ¥980 課金
木曜日: ユーザーD が ¥980 課金

【次の火曜日】
→ あなたの銀行口座に ¥3,780 振込
  （手数料差し引き後）
```

---

## 📊 **収益の確認方法**

### Stripe ダッシュボード

1. **ホーム** → **今日の収益**
2. **支払い** → **すべての支払い**を表示
3. **振込** → 銀行振込履歴を確認

### 銀行口座

- 毎週火曜日に振込
- 振込名義: **STRIPE JAPAN**
- 通帳に記載される

---

## 🚀 **次のステップ**

1. ✅ Stripe テストモードで決済テスト
2. ✅ 本番モードに切り替え
3. ✅ 実際のユーザーに公開
4. 💰 **収益が自動で入り始める**

---

## ❓ **よくある質問**

### Q1: 手数料はいくら？

**A**: 
- 決済手数料: **3.6%**（日本国内カード）
- 海外カード: 4.9%
- 振込手数料: **無料**

### Q2: いつ振り込まれる？

**A**: 
- デフォルト: **毎週火曜日**
- 設定変更可能: 日次、週次、月次

### Q3: 最低振込額は？

**A**: 
- **¥100,000** 以上で自動振込
- それ以下は次回に繰り越し

### Q4: 税金は？

**A**: 
- Stripe は自動で税金を差し引かない
- 年間収益は確定申告が必要
- 経費: サーバー代（¥750/月）、ドメイン代など

---

## 🎯 **最終チェックリスト**

本番公開前に確認：

- [ ] Stripe アカウント作成完了
- [ ] 銀行口座登録完了
- [ ] 本番APIキー取得済み
- [ ] Premium プラン作成済み（価格ID取得）
- [ ] Webhook設定完了（本番URL）
- [ ] 環境変数設定完了（Railway + Cloudflare）
- [ ] テスト決済成功
- [ ] 本番モード有効化
- [ ] 身分証明書提出（必要な場合）

---

**これで完璧です！あとはユーザーが増えれば、自動で銀行口座にお金が入ります！** 🎉💰
